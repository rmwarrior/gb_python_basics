В задании 1 сделал скидку на то, что пользователь вводит корректное число,
  то есть температура выше -273.

В задании 2 было указано завести переменные, поэтому не стал заводить
  константами, но первый порыв был сделать константы.

В задании 3 присутствуют длинные комментарии (больше 80 символов в строке),
  поскольку в настройках Visual Studio Code у меня установлен автоматический
  перенос.

В задании 4 приведены 3 варианта решения:
  -без использования округления и модуля Math (закомментировано);
  -с округлением, без использования модуля Math (закомментировано);
  -с использованием модуля Math (рабочий)
  Вывод строки сознательно не использует форматирования вида `${...}`.
  Выбор функции Floor/Trunc зависит от работы с отрицательными числами.